generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:quyl_db@db.hoauqldsoimvqrqpsygc.supabase.co:5432/postgres?sslmode=require") 
}

model Student {
  id          String   @id @default(uuid())
  studentName String   @map("student_name")
  cohort      String
  courses     String[] 
  joinDate    DateTime @map("join_date")
  lastLogin   DateTime @map("last_login")
  status      String
}
